export interface CrossPackMatch {
  readonly packId: string;
  readonly packLabel: string;
  readonly packType: string;
  readonly system: string;
  readonly id: string;
  readonly name: string;
  readonly documentType?: string;
}
